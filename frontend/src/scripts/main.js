console.log('Main checking in')
let $ = require( 'jQuery' )

// After DOM load
$( document ).ready(  ( DOM ) => {
	// $('.modal-trigger').leanModal();	// Init materialize modal functionality
	console.log('Central')
} )